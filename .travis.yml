notifications:
  slack: oicr:S9k4EowgQv9AnbCfEZHSzCsg
language: java
jdk:
  - oraclejdk8

services:
  - docker

before_install:
- docker pull seqware/seqware_test_environment:develop

script: 
- docker run -ti --rm  -v $HOME/seqware:/mnt/home/seqware/gitroot/seqware seqware/seqware_test_environment:develop /bin/bash -c "cd ~/gitroot/seqware && sudo chown -R seqware:seqware . && mvn -B clean install -DskipITs=false"

# sudo required for docker based testing
sudo: required 
